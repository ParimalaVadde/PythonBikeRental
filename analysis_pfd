# Check all column types
print(transformed_json_df.dtypes)

# Check specific column type
registered_agents_type = dict(transformed_json_df.dtypes)['registered_agents']
print(f"registered_agents column type: {registered_agents_type}")
